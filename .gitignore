# 非公開:vault 根目錄的原作記錄素材(未整理的截圖置き場)
# ※ log/media/原作記録/ は出典を明記した引用として公開するので、先頭の / で root のみ指定
/原作記録/

# 非公開:私的な作業領域(会議メモ、書簡、下書きなど)
private/

# Obsidian の個人設定
.obsidian/

# macOS
.DS_Store

# 非公開:vault 根目錄的原始檔與暫存
/*.mov
/*.MOV
/*.HEIC
/*.zip
/*.pdf
/Pasted image *.png

# 非公開:log 底下只收 jpg / png / mp4,原始檔不進 git
log/media/**/*.HEIC
log/media/**/*.MOV
log/media/**/*.mov
