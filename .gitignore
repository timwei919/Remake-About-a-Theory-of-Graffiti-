# 非公開:原作の記録映像からのスクリーンショット等(著作権が作家・撮影者にあるため)
原作記録/

# 非公開:私的な作業領域(会議メモ、書簡、下書きなど)
private/

# Obsidian の個人設定
.obsidian/

# macOS
.DS_Store

# 非公開:vault 根目錄的原始檔與暫存
/*.mov
/*.MOV
/*.HEIC
/*.zip
/*.pdf
/Pasted image *.png

# 非公開:log 底下只收 jpg / png / mp4,原始檔不進 git
log/media/**/*.HEIC
log/media/**/*.MOV
log/media/**/*.mov
