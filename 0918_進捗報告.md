# 9/18 進捗報告:ハードウェアと制御システム(魏廷宇)

《落書きの原理について》再制作 ／ 前回報告(8/27)からの進捗

---

## 結論から

**プロトタイプが完成しました。** Max/MSPによる全自動制御が通り、**1時間の連続運転で11回の循環**が途切れずに回りました。

> 乾燥の検知 → gcodeの読み込み → 給水しながら描画 → 描画終了の判定 → 送風乾燥 → 原点復帰 → 待機 → 乾燥の検知へ戻る

この一周が人手を介さずに回っています。
→ 記録映像:[開発日誌 9/17](log/開発日誌_ハードウェアと制御システム.md)

---

## この期間の要点

### 1. 原作と同じ機種を特定して購入した

記録映像と写真を突き合わせた結果、原作に使われているのは smartDIYs の **FABOOL Laser Mini** そのもので、アルミフレームとプーリーを延長して大きくしたものだと分かりました。同じ機種を購入しています。

これにより「当時のソフト環境を忠実に再現するか、いまの環境で組み直すか」という分かれ道が自動的に解消しました。
→ [開発日誌 8/14](log/開発日誌_ハードウェアと制御システム.md)

### 2. レーザーの制御信号をポンプの信号に流用した

コントロールボックスを開けて配線を解析し、**レーザーの照射を決めている信号線(白線)**を特定しました。この信号をそのままチューブポンプのスイッチに転用しています。

**「レーザーが焼くはずだった場所に、水が滴る」**——原作の改造の考え方をそのまま実装したことになります。この割り当てのおかげで、生成側はポンプのことを知らなくてよく、ふつうのgcodeを出力するだけで済みます。
→ [開発日誌 8/23・8/28〜29](log/開発日誌_ハードウェアと制御システム.md)

### 3. 当時の制御ソフトを見つけて、いまのMacで動かした

「Smart Laser CO2」のブラウザ版ソフト(サーバーを起動して `127.0.0.1:4444` にアクセスする方式)が見つかりました。原作の機材リストに Chrome があった理由もこれで分かりました。**ブラウザが操作画面そのもの**で、原作のマウス自動化はウェブページをクリックしていたことになります。

macOS 12.3以降はシステムにPython 2がないため、miniforgeでPython 2.7の環境を作り、同梱された2015年のライブラリに署名を付け直して起動しました。接続すると `# FaboolLaserMini 3.00` と表示され、**2015年のソフトで現在のFABOOLを動かせる**ことが確認できています。

この四つの手順は、原作のソフト環境を現在の環境で再構築するための肝なので、レシピに残す予定です。
→ [開発日誌 9/16](log/開発日誌_ハードウェアと制御システム.md)

### 4. ファイルの受け渡しの形式が確定した

記録映像の画面に写っているファイル名が `gcode.ngc` であることを確認しました。しかも**毎回、同じファイルが上書きされて読み直されている**。

ファイル名もパスも変わらないので、ファイル選択ダイアログを開けば毎回同じ位置にファイルがある——これが座標を固定したマウス自動化が安定して動く理由です。

→ **藁科さんとのインターフェースは「固定のパス・固定のファイル名・毎回上書き」で確定**とさせてください。
なお、重ね描き(濃淡の制御)はソフト側の機能なので、生成側では気にしなくて大丈夫です。
→ [開発日誌 9/16](log/開発日誌_ハードウェアと制御システム.md)

### 5. ノズルの構成が分かった

透明なアクリル管の中にシリコンチューブが通っている構造でした。チューブが中にあるので半透明に見え、アクリルが外にあるので真っ直ぐに見える——二回の設置写真で見え方が違っていた理由もこれで解けました。

同じ構成にしたところ、給水の位置がはっきり安定しました。
→ [開発日誌 9/15・9/17](log/開発日誌_ハードウェアと制御システム.md)

### 6. 描画のパラメータを詰めた

| 項目 | 値 |
| --- | --- |
| 速度 | 2000 mm/min |
| 出力 | 30% |

速度2500以上では点状に途切れ、出力30を切ると水がほとんど出ません。つまり**使える範囲がかなり狭い**ということでもあります。本番サイズでは測り直しになります。
→ [開発日誌 9/13・9/14](log/開発日誌_ハードウェアと制御システム.md)

---

## いま分かっている重要な点

**乾燥が循環の長さを決めています。** 描画そのものは数分で終わりますが、水が乾くまでの時間はそれよりずっと長い。扇風機は周辺の部品ではなく、循環が成り立つかどうかを左右する本体です。

**ブロックの厚みは作品の同一性の一部ではありません。** 二回の展示で違うブロックが使われていました(NO-MA は 390×190×100、OIST は 390×190×150)。作家自身が動かした範囲なので、こちらも選択の理由を記録したうえで決めればよいことになります。
→ [開発日誌 9/16](log/開発日誌_ハードウェアと制御システム.md)

**制御の条件はすべて画面上の数値で変更できます。** 湿度の閾値、待機時間、マウスの座標、各段階の待ち時間。本番サイズになったら入れ直すだけで済むようにしてあります。

---

## 次にやること

1. アクリル管の台座を3Dプリントする(いまは固定が甘く、外から触れるとずれる)
2. 湿度センサーを挟むクリップを探す(原作もクリップで留めている)
3. Maxの表示画面を原作に近づける
4. 真上から撮るWebカメラを足す
5. **本制作に入る。アルミフレームの延長**
6. 原作に近い扇風機とタンクを探す

あわせて、**数時間から半日の連続運転**を一度やっておきたいと考えています。1時間では出てこない問題——センサーの電極に水のミネラルが付いて読み値がずれる、ブロックに水が溜まって乾燥時間が伸びる——は、会期が三日あることを考えると先に確かめておくべきだと思っています。

---

## 記録の所在

開発の経緯は日付ごとの開発日誌にまとめてあります。写真と映像も各項目に入っています。

- [開発日誌(日本語)](log/開発日誌_ハードウェアと制御システム.md)
- [開発日誌(中国語)](log/開発日誌_硬體與控制系統.md)

※ 原作の記録映像からのキャプチャは、著作権が作家・撮影者に帰属するためリポジトリには含めていません。該当箇所にはその旨を記してあります。

コードは以下にあります。

- Arduino:`private/arduino/GraffitiIO/GraffitiIO.ino`
- Max/MSP:`private/max/GraffitiControl.maxpat`
